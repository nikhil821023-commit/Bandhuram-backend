spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bandhuram
    username: postgres
    password: Nikhil@86
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB


server:
  port: 8080



app:
  jwt:
    secret: ${JWT_SECRET:MyVeryStrongSecretKeyForJWT123456789}
    expiration-ms: 86400000   # 24 hours
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000,http://127.0.0.1:3000}
  admin:
    default-username: ${ADMIN_USERNAME:admin}
    default-password: ${ADMIN_PASSWORD:Bandhuram@2026}

  upload:
    dir: ${UPLOAD_DIR:./uploads/shop-images}